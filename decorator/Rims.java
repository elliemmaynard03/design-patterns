/*
 * Written by Ellie Maynard
 */
package decorator;

public class Rims {

    /*
     * reads the car.txt file and the rims.txt file
     */
    public Rims(Vehicle vehicle) {
        this.vehicle = vehicle;
        this.lines = super.FileReader.getLines('decorator/car.txt');
        this.result = super.FileReader.getLines('decorator/rims.txt');

        if(lines.get[i] =' ') {
            result
        }
    }
}
